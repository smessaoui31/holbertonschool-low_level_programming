#include <stdio.h>
/**
 * main - Prints if numbers
 *
 * Return: Always (Success)
 */

int main(void)
{
	int num;

	for (num = 0 ; num < 10 ; num++)
	{
		printf("%d", num);
	}
	putchar('\n');
	return (0);
}
