#include <stdio.h>

	/** Main = Entry point of our command
	 *
	 * Use command puts to print 
	 *
	 * Return :Always return to c (success)
	 * */
	int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
