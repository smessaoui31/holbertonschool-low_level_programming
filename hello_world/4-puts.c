#include <stdio.h>

	/**
	 * main - Entry point
	 *
	 * Use command puts to print
	 *
	 * Return :Always return to 0 (success)
	 */
	int main(void) /*Main is the entry point*/
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
